apply plugin: "war"

bootWar {
    mainClassName = "com.terabizcloud.messaging.MessagingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
